/**
 * ======================= CONFIGURATION =======================
 * Optional teacher settings. Git Quest works with this file untouched —
 * everything below only affects the "Report to lecturer" panel.
 */
const CONFIG = {
  /**
   * Google Apps Script Web App URL that records each student's stars into a
   * spreadsheet. Leave as "" and the panel simply says it is not configured.
   * See google-apps-script/Code.gs for the script and the setup steps.
   */
  SHEETS_WEBAPP_URL: ""
};
